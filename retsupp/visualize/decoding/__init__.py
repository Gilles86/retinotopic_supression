"""Visualization helpers for the StimulusFitter decoding pipeline.

Modules:
- :mod:`make_decoded_paradigm_movies` -- MP4 movies of decoded (T, R, R)
  tensors. Used for both single-run smoke movies and the 2E/2F group
  outputs (mean-decoded per ROI, event-locked rotated grid).
- :mod:`plot_rotated_decoded_heatmaps` -- static rotated panels (plan §5a).
- :mod:`plot_hp_vs_lp_decoded` -- HP-vs-LP scatter (plan §5c).
"""
